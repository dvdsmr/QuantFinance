#include "fft.h"
#include <vector>

namespace FFT
{
	auto fft(const auto& model, const std::vector<double>& params) -> std::vector<double>
	{
		// under construction
		std::vector<double> prices{};

		return prices;
	}
}